# Tests

This directory contains validation checks for the Code Smell Detective skill package.

The tests are intentionally lightweight. They verify that required files exist and that the skill metadata is present.
